# 📝 Git Ignore - Информация

## Что игнорируется и почему

### 🔧 Claude Code (`.claude/`)
**Игнорируется:** Вся папка `.claude/`
**Причина:** Персональные настройки и кеш Claude Code

### 📦 Датасет (`dataset/`)
**Игнорируется:**
- `dataset/images/` - все изображения (~1.5 GB)
- `dataset/labels/` - все аннотации
- `dataset/*.cache` - кеш файлы YOLO

**Сохраняется:**
- `dataset/data.yaml` - конфигурация датасета (важно!)
- Структура папок

**Причина:** Изображения занимают много места в Git. Датасет можно скачать отдельно с Roboflow.

### 🏋️ Результаты обучения (`runs/`)
**Игнорируется:**
- Вся папка `runs/` с результатами обучения
- Модели `*.pt` (~6 MB каждая)
- Все чекпоинты

**Причина:** Модели большие и генерируются при обучении. Лучшую модель можно сохранить отдельно.

### 📊 Выходные данные (`outputs/`)
**Игнорируется:**
- `outputs/plots/*.png` - графики
- `outputs/predictions/*` - визуализации
- `outputs/videos/*` - обработанные видео

**Причина:** Генерируются автоматически при запуске notebook.

### 🎥 Видео
**Игнорируется:** Все видеофайлы (`*.mp4`, `*.avi`, etc.)
**Причина:** Видео очень большие (сотни MB).

### 🐍 Python
**Игнорируется:**
- `__pycache__/` - Python cache
- `*.pyc` - скомпилированные файлы
- `venv/`, `env/` - виртуальные окружения

**Причина:** Генерируются автоматически, не нужны в Git.

### 📓 Jupyter
**Игнорируется:**
- `.ipynb_checkpoints/` - автосохранения Jupyter

**Причина:** Временные файлы.

---

## 📥 Что включено в Git

**Сохраняется в репозитории:**
- ✅ `README.md` - документация
- ✅ `requirements.txt` - зависимости
- ✅ `homework_3_fast.ipynb` - оптимизированный notebook
- ✅ `homework_3_object_detection.ipynb` - исходный notebook
- ✅ `*.py` - Python скрипты
- ✅ `dataset/data.yaml` - конфигурация датасета
- ✅ `.gitignore` - этот файл

---

## 🔄 Как восстановить проект

### 1. Клонировать репозиторий:
```bash
git clone <repository-url>
cd hw_3
```

### 2. Установить зависимости:
```bash
pip install -r requirements.txt
```

### 3. Скачать датасет:

**Вариант A: С Roboflow (рекомендуется)**
```python
from roboflow import Roboflow
rf = Roboflow(api_key="YOUR_API_KEY")
project = rf.workspace("project-h68de").project("exdark-kd37x")
version = project.version(12)
dataset = version.download("yolov8")
```

**Вариант B: Из облака**
- Скачайте `dataset.zip` из облачного хранилища
- Распакуйте в папку `dataset/`

### 4. Запустить обучение:
```bash
jupyter notebook homework_3_fast.ipynb
```

---

## 📊 Размеры файлов

**Игнорируемые файлы (не в Git):**
- Dataset images: ~1.5 GB
- Dataset labels: ~5 MB
- Model weights: ~6 MB
- Training results: ~50 MB
- Videos: ~100-500 MB каждое
- **Итого игнорируется: ~2+ GB**

**Включенные файлы (в Git):**
- Code + notebooks: ~500 KB
- Config files: ~10 KB
- README: ~20 KB
- **Итого в Git: ~1 MB**

---

## 🎯 Рекомендации

### ✅ Хорошая практика:
- Коммитить код, конфигурацию и документацию
- Игнорировать большие файлы (модели, датасеты, видео)
- Использовать Git LFS для больших файлов если необходимо

### ❌ Плохая практика:
- Коммитить датасет в Git (делает репозиторий огромным)
- Коммитить виртуальные окружения
- Коммитить временные файлы

---

## 📝 Дополнительно

**Если нужно сохранить обученную модель:**
1. Используйте Git LFS:
```bash
git lfs install
git lfs track "*.pt"
git add runs/detect/yolov8n_fast/weights/best.pt
```

2. Или загрузите в облако (Google Drive, Яндекс.Диск):
```bash
# Пример для gdown
gdown --id YOUR_FILE_ID
```

**Если датасет изменился:**
1. Обновите `dataset/data.yaml`
2. Закоммитьте изменения:
```bash
git add dataset/data.yaml
git commit -m "Update dataset configuration"
```

---

**📌 Этот файл создан автоматически для объяснения `.gitignore`**
